export default function CollectionsPage({ collections, cards }) {
  console.log(collections, cards);
  return (
    <>
      <h1>hallo, hier kommen die collections rein</h1>
      <p>
        ich muss dafür die create und edit page anpassen und auch die
        Layout-Komponente, sodass die Navigationbar auf allen relevanten pages
        einzelnd returned wird, aber z.B. auf der editpage nicht
      </p>
    </>
  );
}
