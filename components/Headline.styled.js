import styled from "styled-components";

export const StyledHeadlines = styled.h3`
  text-align: center;
  font-size: calc(min(2rem, 3vh));
  color: var(--color-font-1);
  opacity: 95%;
  margin: 1vh;
  margin-bottom: 1vh;
`;
